# MemeToken - SHIB 风格 Meme 代币智能合约

一个功能完整的基于以太坊的 Meme 代币智能合约，类似 SHIB（Shiba Inu），包含交易税、流动性池集成和交易限制等高级功能。

## 📋 目录

- [功能特性](#功能特性)
- [合约架构](#合约架构)
- [环境准备](#环境准备)
- [部署指南](#部署指南)
- [使用指南](#使用指南)
- [合约功能详解](#合约功能详解)
- [安全注意事项](#安全注意事项)
- [常见问题](#常见问题)

## 🎯 功能特性

### 1. 代币税功能
- **买入税**: 可配置的买入税率（默认 5%）
- **卖出税**: 可配置的卖出税率（默认 5%）
- **税费分配**: 税费自动分配给指定的税费钱包地址
- **灵活配置**: 支持动态调整税率（最高 15%）
- **排除机制**: 可以排除特定地址（如 DEX 路由、流动性池）的税费

### 2. 流动性池集成
- **自动集成**: 与 Uniswap V2 兼容的 DEX 自动集成
- **添加流动性**: 支持通过合约直接向流动性池添加代币和 ETH
- **移除流动性**: 支持从流动性池移除流动性
- **LP 代币管理**: 自动管理流动性提供者（LP）代币

### 3. 交易限制功能
- **单笔交易限额**: 限制单笔交易的最大数量，防止大额恶意交易
- **持仓限额**: 限制单个地址的最大持仓量
- **每日交易限额**: 限制每个地址每日的交易总量
- **灵活控制**: 可以启用/禁用限制，或排除特定地址

### 4. 安全特性
- **重入攻击防护**: 使用 ReentrancyGuard 防止重入攻击
- **权限控制**: 使用 Ownable 进行管理员权限管理
- **紧急功能**: 支持紧急提取 ETH 和代币
- **交易控制**: 可以启用/禁用交易功能

## 🏗️ 合约架构

```
MemeToken.sol
├── ERC20 标准功能
│   ├── 转账
│   ├── 余额查询
│   └── 授权
├── 交易税系统
│   ├── 买入税计算
│   ├── 卖出税计算
│   ├── 税费分配
│   └── 排除列表管理
├── 流动性池集成
│   ├── 添加流动性
│   ├── 移除流动性
│   └── LP 代币管理
├── 交易限制系统
│   ├── 单笔限额检查
│   ├── 持仓限额检查
│   └── 每日限额检查
└── 管理员功能
    ├── 参数配置
    ├── 地址管理
    └── 紧急控制
```

## 🔧 环境准备

### 前置要求

- Node.js >= 16.0.0
- npm 或 yarn
- Git

### 安装依赖

```bash
# 克隆或下载项目
cd meme

# 安装依赖
npm install
```

### 环境变量配置

创建 `.env` 文件（可选，用于保护私钥）：

```env
# 部署账户私钥（不要提交到 Git）
PRIVATE_KEY=your_private_key_here

# 税费钱包地址
TAX_WALLET=0xYourTaxWalletAddress

# Uniswap Router 地址（根据网络选择）
# 以太坊主网: 0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D
# 以太坊 Sepolia 测试网: 0xC532a74256D3Db42D0Bf7a0400fEFDbad7694008
# BSC 主网: 0x10ED43C718714eb63d5aA57B78B54704E256024E
# BSC 测试网: 0xD99D1c33F9fC3444f8101754aBC46c52416550D1
UNISWAP_ROUTER=0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D

# RPC URL（用于主网/测试网部署）
MAINNET_RPC_URL=your_mainnet_rpc_url
SEPOLIA_RPC_URL=your_sepolia_rpc_url
BSC_RPC_URL=your_bsc_rpc_url

# 区块浏览器 API 密钥（用于合约验证）
ETHERSCAN_API_KEY=your_etherscan_api_key
BSCSCAN_API_KEY=your_bscscan_api_key
```

## 🚀 部署指南

### 1. 编译合约

```bash
npm run compile
```

### 2. 配置部署参数

编辑 `scripts/deploy.js`，修改以下参数：

```javascript
// 代币基本信息
const TOKEN_NAME = "YourTokenName";
const TOKEN_SYMBOL = "YOUR";
const TOTAL_SUPPLY = ethers.parseEther("1000000000"); // 10 亿代币

// 税费设置（基点，100 = 1%）
const BUY_TAX_RATE = 500;   // 5%
const SELL_TAX_RATE = 500;  // 5%

// 交易限制（根据你的需求调整）
const MAX_TRANSACTION_AMOUNT = ethers.parseEther("10000000");
const MAX_WALLET_AMOUNT = ethers.parseEther("50000000");
const DAILY_TRADING_LIMIT = ethers.parseEther("50000000");
```

### 3. 部署到本地网络（测试）

```bash
# 启动本地 Hardhat 节点
npx hardhat node

# 在另一个终端部署
npm run deploy:local
```

### 4. 部署到测试网

```bash
# 确保已设置环境变量
npm run deploy:testnet
```

### 5. 部署到主网

⚠️ **警告**: 主网部署会消耗真实资金，请谨慎操作！

```bash
# 确保已设置环境变量并仔细检查所有参数
npm run deploy:mainnet
```

部署成功后，会生成 `deployment-info.json` 文件，包含合约地址和部署信息。

## 📖 使用指南

### 初始化步骤

1. **部署合约**
   ```bash
   npm run deploy:local  # 或 testnet/mainnet
   ```

2. **添加初始流动性**
   ```javascript
   // 在部署后，需要添加流动性才能开始交易
   // 示例：添加 1000 万代币和 1 ETH 的流动性
   const tokenAmount = ethers.parseEther("10000000");
   const ethAmount = ethers.parseEther("1");
   const deadline = Math.floor(Date.now() / 1000) + 300; // 5分钟后
   
   await memeToken.addLiquidity(tokenAmount, ethAmount, deadline);
   ```

3. **启用交易**
   ```javascript
   await memeToken.enableTrading();
   ```

### 代币交易

#### 通过 DEX（如 Uniswap）交易

1. 访问 Uniswap 或其他兼容的 DEX
2. 连接钱包
3. 选择你的代币进行交易
4. 确认交易（会自动扣除税费）

#### 通过合约直接转账

```javascript
// 转账代币（会自动扣除税费）
await memeToken.transfer(recipientAddress, amount);
```

### 添加流动性

#### 方法 1: 使用合约的 addLiquidity 函数

```javascript
// 准备参数
const tokenAmount = ethers.parseEther("1000000"); // 100 万代币
const ethAmount = ethers.parseEther("0.1");       // 0.1 ETH
const deadline = Math.floor(Date.now() / 1000) + 300; // 5分钟后

// 调用添加流动性（需要先授权代币）
await memeToken.addLiquidity(tokenAmount, ethAmount, deadline, {
  value: ethAmount
});
```

#### 方法 2: 使用 addLiquidityETH 函数

```javascript
// 直接发送 ETH，合约会自动使用发送者的代币余额
await memeToken.addLiquidityETH({
  value: ethers.parseEther("0.1") // 发送 0.1 ETH
});
```

#### 方法 3: 通过 Uniswap 前端

1. 访问 Uniswap 的添加流动性页面
2. 选择你的代币和 ETH
3. 输入数量并确认

### 移除流动性

```javascript
// 获取流动性代币数量
const lpToken = await ethers.getContractAt("IERC20", pairAddress);
const liquidity = await lpToken.balanceOf(yourAddress);

// 移除流动性
const deadline = Math.floor(Date.now() / 1000) + 300;
await memeToken.removeLiquidity(liquidity, deadline);
```

### 管理员操作

#### 设置税费

```javascript
// 设置买入税 3%，卖出税 5%
await memeToken.setTaxRates(300, 500); // 基点：300 = 3%, 500 = 5%
```

#### 设置交易限制

```javascript
// 设置交易限制
await memeToken.setLimits(
  ethers.parseEther("5000000"),  // 单笔最大 500 万
  ethers.parseEther("20000000"), // 单个地址最大 2000 万
  ethers.parseEther("10000000")  // 每日限额 1000 万
);
```

#### 排除地址的税费

```javascript
// 排除某个地址的税费
await memeToken.setTaxExclusion(address, true);

// 取消排除
await memeToken.setTaxExclusion(address, false);
```

#### 启用/禁用交易限制

```javascript
// 禁用交易限制
await memeToken.setLimitsEnabled(false);

// 重新启用
await memeToken.setLimitsEnabled(true);
```

#### 启用/禁用交易

```javascript
// 禁用交易（紧急情况）
await memeToken.disableTrading();

// 重新启用
await memeToken.enableTrading();
```

## 🔍 合约功能详解

### 税费机制

#### 工作原理

1. **买入税**: 当用户从 DEX 购买代币时，自动扣除买入税
2. **卖出税**: 当用户向 DEX 卖出代币时，自动扣除卖出税
3. **税费分配**: 所有税费自动发送到 `taxWallet` 地址
4. **排除机制**: 被排除的地址（如 DEX 路由、流动性池）不收取税费

#### 税费计算

```solidity
// 买入税 = 交易金额 × 买入税率 / 10000
uint256 taxAmount = (amount * buyTaxRate) / 10000;
// 实际接收 = 交易金额 - 税费
uint256 transferAmount = amount - taxAmount;
```

### 交易限制机制

#### 单笔交易限额

- 限制每笔交易的最大数量
- 仅对与交易对的交易生效
- 可以排除特定地址

#### 持仓限额

- 限制单个地址的最大代币持有量
- 防止巨鲸过度持仓
- 仅在买入时检查

#### 每日交易限额

- 跟踪每个地址每日的交易总量
- 每天 UTC 0:00 自动重置
- 防止频繁交易操纵价格

### 流动性池集成

合约自动与 Uniswap V2 兼容的 DEX 集成：

1. **自动识别交易对**: 自动识别代币/ETH 交易对
2. **添加流动性**: 支持通过合约直接添加
3. **移除流动性**: 支持从合约移除流动性
4. **LP 代币管理**: 自动处理流动性提供者代币

## ⚠️ 安全注意事项

### 部署前检查清单

- [ ] 仔细检查所有参数（税率、限额等）
- [ ] 确保税费钱包地址正确
- [ ] 验证 Uniswap Router 地址与网络匹配
- [ ] 测试合约功能（在测试网上）
- [ ] 审查合约代码和权限设置
- [ ] 确保私钥安全（不要提交到 Git）

### 运行时安全

1. **私钥保护**: 永远不要将私钥提交到代码仓库
2. **权限管理**: 定期审查管理员权限
3. **参数调整**: 调整重要参数前在测试网验证
4. **紧急功能**: 了解如何使用紧急功能（禁用交易、提取资金等）
5. **监控**: 定期监控合约状态和交易

### 常见风险

- **重入攻击**: 合约已使用 ReentrancyGuard 防护
- **整数溢出**: 使用 Solidity 0.8+ 自动检查
- **权限滥用**: 确保管理员地址安全
- **税费设置错误**: 设置过高的税率可能影响交易

## ❓ 常见问题

### Q1: 如何修改税率？

A: 部署后，合约所有者可以调用 `setTaxRates(buyTax, sellTax)` 修改税率。税率以基点表示（100 = 1%），最高 15%。

### Q2: 税费是如何分配的？

A: 所有税费自动发送到 `taxWallet` 地址。你可以通过 `setTaxWallet()` 更改税费钱包地址。

### Q3: 如何添加流动性？

A: 有三种方式：
1. 使用合约的 `addLiquidity()` 函数
2. 使用合约的 `addLiquidityETH()` 函数
3. 通过 Uniswap 前端手动添加

### Q4: 交易限制可以关闭吗？

A: 可以。合约所有者可以调用 `setLimitsEnabled(false)` 关闭所有交易限制。

### Q5: 如何排除某个地址的税费？

A: 合约所有者可以调用 `setTaxExclusion(address, true)` 排除地址。

### Q6: 部署后需要做什么？

A: 
1. 添加初始流动性（推荐通过 Uniswap 前端）
2. 调用 `enableTrading()` 启用交易
3. 在 DEX 上创建/验证交易对

### Q7: 支持哪些网络？

A: 合约支持所有 EVM 兼容的网络，包括：
- 以太坊主网/测试网
- BSC 主网/测试网
- Polygon
- Arbitrum
- Optimism
- 其他 EVM 兼容链

### Q8: 如何验证合约？

A: 部署脚本会自动尝试验证合约。如果失败，可以手动验证：

```bash
npx hardhat verify --network <network> <contract_address> <constructor_args>
```

## 📝 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，请通过 GitHub Issues 联系。

---

**免责声明**: 本合约仅供学习和研究使用。在实际部署前，请进行充分的安全审计和测试。作者不对任何因使用本合约而造成的损失负责。

